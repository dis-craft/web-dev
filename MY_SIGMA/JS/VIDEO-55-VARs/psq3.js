//create a const object in javascript can u change it to hold a number later?

const myObject = {
    id: 20,
    name: "Srikar"
};

console.log(myObject);

myObject.id = 23; //modifying
myObject.marks = 99; //adding new key
console.log(myObject);
